<policies>
    <inbound>
        <base />

        <!-- Determine if the client certificate is valid and store the reason in a variable -->
        <set-variable name="certificateValidationResult" value="@{
            if (context.Request.Certificate == null)
            {
                return "ClientCertificateNotFound";
            }

            var now = DateTime.Now; // We are using DateTime.Now because NotBefore and NotAfter are in local time
            if (context.Request.Certificate.NotBefore > now)
            {
                return "ClientCertificateNotYetValid";
            }
            if (context.Request.Certificate.NotAfter < now)
            {
                return "ClientCertificateExpired";
            }

            if ({{validate-certificate-chain}} && !context.Request.Certificate.VerifyNoRevocation())
            {
                return "ClientCertificateNotTrusted";
            }

            if (!context.Deployment.Certificates.Any(c => c.Value.Thumbprint == context.Request.Certificate.Thumbprint))
            {
                return "ClientCertificateIdentityNotMatched";
            }

            return null;
        }" />

        <choose>
            <!-- Trace and return a 401 Unauthorized if the client certificate is not valid -->
            <when condition="@(context.Variables.GetValueOrDefault<string>("certificateValidationResult") != null)">
                <trace source="validate-using-context" severity="error">
                    <message>@("Client certificate validation failed: " + context.Variables.GetValueOrDefault<string>("certificateValidationResult"))</message>
                </trace>
                <return-response>
                    <!-- Include error reason for demo purposes. Don't expose sensitive information in production and use a generic error message instead. -->
                    <set-status code="401" />
                    <set-header name="ErrorReason" exists-action="override">
                        <value>@(context.Variables.GetValueOrDefault<string>("certificateValidationResult"))</value>
                    </set-header>
                </return-response>
            </when>
        </choose>

        <!-- Returns a 200 OK with the client certificate details in the body -->
        <return-response>
            <set-status code="200" />
            <set-body>@(context.Request.Certificate.ToString())</set-body>
        </return-response>
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <base />
    </outbound>

    <on-error>
        <!-- Include error details in the response headers. Don't expose sensitive information in production. -->
        <set-header name="ErrorSource" exists-action="override">
            <value>@(context.LastError.Source)</value>
        </set-header>
        <set-header name="ErrorReason" exists-action="override">
            <value>@(context.LastError.Reason)</value>
        </set-header>
        <set-header name="ErrorMessage" exists-action="override">
            <value>@(context.LastError.Message)</value>
        </set-header>
        <base />
    </on-error>
</policies>
